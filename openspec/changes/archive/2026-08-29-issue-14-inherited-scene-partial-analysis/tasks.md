## 1. Retain and Apply Inheritance Evidence

- [x] 1.1 Retain parser editable signals and override presence in owned local/inherited evidence; verify local summary tests cover root classification, stubs, typed additions, mounts, and `[editable ...]`.
- [x] 1.2 Relax root completeness coverage so inherited occurrences remain independent of scene-instance coverage; verify root and repeated inherited coverage validate while negative values still fail.
- [x] 1.3 Add transactional inherited-base application for occurrence metrics, maximum depth, coverage/evidence, resources, and dependencies without a mount/root increment; verify overflow leaves the builder unchanged.
- [x] 1.4 Replace deferred inherited sentinel handling with root-level resolved and unsupported base expansion; verify resolved bases apply once and missing/imported/unreadable bases retain exactly one known root.
- [x] 1.5 Preserve structured base classification/reason and grouped `SB1003` evidence through transitive and repeated cached summaries; verify approximate reliability and occurrence counts.
- [x] 1.6 Keep production changes within `internal/analysis` with no override merge, parser syntax, resolver, graph, CLI, report, config, budget, preset, dependency, process, Godot, or network changes; verify the production diff boundary.

## 2. Commit the Production Feature

- [x] 2.1 Format production files and run focused local/recursive/completeness tests plus `go build ./...`, `go test ./...`, and `go vet ./...`; verify existing non-inherited and fatal behavior remains green.
- [x] 2.2 Commit only non-test implementation files as `feat: analyze inherited scenes approximately`; verify no `*_test.go`, OpenSpec progress, or unrelated files enter the commit.

## 3. Cover Frozen Inheritance Semantics

- [x] 3.1 Add resolved base fixtures with explicit local typed nodes, nested mounts, override stubs, and editable entries; verify base/local formulas, no duplicate root, and no inheritance scene-instance increment.
- [x] 3.2 Add missing, unreadable, imported, binary, `SubResource`, UID/user, outside-project, and unsupported base fixtures; verify one known root, structured evidence, and `partial approximate`.
- [x] 3.3 Add root, nested, transitive, repeated ×100, and diamond inheritance fixtures; verify one-occurrence cache reuse, multiplied metrics/evidence, unique resources/dependencies, parsed coverage, and ownership.
- [x] 3.4 Add inherited cycle, malformed supported base, metric/coverage/evidence overflow, and caller-mutation fixtures; verify every fatal path returns a zero result and cached state remains unchanged.

## 4. Verify and Deliver Tests

- [x] 4.1 Run focused inheritance/local/recursive/graph/completeness tests plus `go test ./...`, `go test -race ./...`, `go vet ./...`, and CI-pinned golangci-lint v2.12.0; verify all gates pass deterministically.
- [x] 4.2 Commit only `*_test.go` files and OpenSpec task progress as `test: cover inherited scene analysis`; verify feature and test commits remain independently reviewable.
- [x] 4.3 Run strict OpenSpec validation and `git diff --check`, inspect Draft PR #39 through the GitHub connector, and verify the diff stays within #14 planning, analysis inheritance, and tests before archive/merge.
