## 1. Confidence Domain

- [x] 1.1 Add the fixed eight-metric confidence model, stable reason taxonomy, canonical merge/access/validation helpers, and deep-clone support; verify focused analysis package tests pass.
- [x] 1.2 Derive root confidence from unresolved scene, ordinary resource, inherited scene, and unsupported parent evidence and verify report-wide reliability/status are conservative summaries.
- [x] 1.3 Derive, merge, scale, and clone contribution metric confidence while retaining row-wide reliability and unavailable maximum/unique-union values; verify contribution reconciliation and ownership tests pass.

## 2. Report Projections

- [x] 2.1 Add confidence objects to root and contribution JSON metric records, update the schema-v1 contract, and verify inspect/check/tree documents validate against it.
- [x] 2.2 Format inspect metrics, check actuals, and top-contribution values with metric-level confidence and render concise mixed-confidence qualifications; verify uniform reports avoid redundant sections.
- [x] 2.3 Update user documentation and checked-in report examples to describe reason codes, mixed confidence, and schema-v1 compatibility.

## 3. Verification and Delivery

- [x] 3.1 Add focused tests for all eight metrics, every supported evidence class, deterministic reason order, invalid metadata, clone ownership, and unavailable-value handling; verify targeted packages pass.
- [x] 3.2 Regenerate deterministic CLI/JSON goldens and verify repeated text and JSON rendering is byte-identical and portable.
- [x] 3.3 Run `go build ./...`, `go test ./...`, `go test -race ./...`, `go vet ./...`, `golangci-lint run`, and `openspec validate issue-54-add-per-metric-confidence --strict`; record the successful commands before archive.
