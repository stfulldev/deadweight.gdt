# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Initialize the Go CLI and domain model.
- Add the experimental built-in `mobile`, `steam-deck`, and `desktop` presets.
- Add `presets` and `presets show` commands.
- Add a streaming Godot 4 TSCN subset lexer and parser.
- Detect malformed scenes, duplicate external-resource IDs, inherited roots, override nodes, and editable sections.
- Add parser unit, large-value, malformed-input, and fuzz tests.
