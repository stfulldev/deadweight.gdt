# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Initialize the Go CLI and domain model.
- Add the experimental built-in `mobile`, `steam-deck`, and `desktop` presets.
- Add `presets` and `presets show` commands.
